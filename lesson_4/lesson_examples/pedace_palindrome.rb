=begin
input: string
output: string
rules: 
      Explicit requirements:
      - every palindrome in the string must be converted to uppercase.
      (Reminder: a palindrome is a word that reads the same forwards and backward.)
      - Palindromes are case sensitive('Dad' is not a palindrome but 'dad' is)

      Implicit requirements:
      - teh returned string shouldn't be teh same string objects.
      - if the string is an empty string, the result should be an empty array


=end